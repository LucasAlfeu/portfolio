import { listaDeProjetos } from 'data/portifolio';

export interface Projetos {
    nome: string
    descricao: string
    resumo: string
    link: string
    imagem: string
    id: number
    categoria: string
    codigo: string
    busca: string
}